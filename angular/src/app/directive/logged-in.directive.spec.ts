import { LoggedInDirective } from './logged-in.directive';


describe('LoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = LoggedInDirective;
    expect(directive).toBeTruthy();
  });
});
